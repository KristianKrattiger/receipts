{
  "name": "receipts",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "cli": "tsx --env-file-if-exists=.env src/cli/index.ts",
    "mcp": "tsx --env-file-if-exists=.env src/mcp/server.ts",
    "serve": "tsx --env-file-if-exists=.env src/web/server.ts",
    "site": "tsx src/web/build.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "@modelcontextprotocol/sdk": "^1.20.0",
    "@solarisdk/browser": "^0.1.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
